Q1)JPQL: Instructions: A) Create an employeeTable table with the following fields: empId, empFirstName, empLastName,
empSalary, empAge. B) Create an Employee entity having following fields: id, firstName, lastName, salary, age which
maps to the table columns given in above. Questions:

1)Display the first name, last name of all employees having salary greater than average salary ordered in ascending
by their age and in descending by their salary.
2)Update salary of all employees by a salary passed as a parameter whose existing salary is less than the average salary.
3) Delete all employees with minimum salary.

Q2)Native Query: Instructions:
1) Display the id, first name, age of all employees where last name ends with "singh"
2) Delete all employees with age greater than 45(Should be passed as a parameter)